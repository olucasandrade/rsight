---
phase: 03-ai-conversations-open-actions
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - AICV-01
  - AICV-02
  - AICV-03
  - AICV-04
  - OPEN-01
  - OPEN-02

must_haves:
  truths:
    - "AI Conversations tab shows real results when user types a query that matches conversation content"
    - "Each result displays as 'title · date [Source]' with query matches highlighted"
    - "Pressing Enter on a Claude Code result opens a new Terminal.app window with claude --resume"
    - "File and folder open actions have no regression"
    - "Search performance is acceptable (results appear without excessive delay)"
  artifacts:
    - path: "src/ai_search/claude.rs"
      provides: "Functional Claude Code parser"
    - path: "src/ai_search/cursor.rs"
      provides: "Functional Cursor parser"
  key_links: []
---

<objective>
Verify Phase 3 end-to-end in the running TUI. A human confirms that AI conversation search works, results display correctly, and open actions behave as designed.

Purpose: Human eyes on the working product — automated tests can't verify TUI rendering, new Terminal.app window spawning, or the real-world conversation data.
Output: Phase 3 confirmed complete or gaps identified for follow-up.
</objective>

<execution_context>
@/Users/lucasandrade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lucasandrade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run rsight and confirm it launches cleanly</name>
  <files></files>
  <action>
    Build and run rsight:
    ```bash
    cd /Users/lucasandrade/rsight && cargo build --release 2>&1 | tail -5
    cargo run --release
    ```
    Confirm the TUI launches, shows the search bar, and "AI Conversations" appears in the tab bar (not grayed out).
  </action>
  <verify>
    <automated>cd /Users/lucasandrade/rsight && cargo build --release 2>&1 | grep -E "^error" | head -10 || echo "RELEASE BUILD OK"</automated>
  </verify>
  <done>Release build succeeds; rsight launches without panic</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 3 implementation:
    - AI conversation search (Claude Code ~/.claude/projects/ + Cursor ~/.cursor/chats/)
    - "title · date [Source]" display with yellow match highlighting
    - Enter on Claude Code result: new Terminal.app window running `claude --resume <id>`
    - Enter on Cursor result: Cursor opens (or error shown in status bar)
    - File ($EDITOR) and folder (Finder) open actions unchanged
  </what-built>
  <how-to-verify>
    Run `cargo run` from /Users/lucasandrade/rsight, then:

    1. **AI Conversations tab visible:** Tab bar shows "Files | Folders | Contents | AI Conversations" — all tabs selectable with Tab key
    2. **Claude Code search:** Type a word from a recent conversation (e.g., "gsd" or "rsight" or "plan"). Press Tab until AI Conversations is active. Confirm results appear with "title · date [Claude]" format.
    3. **Cursor search:** If ~/.cursor/ exists, type a word from a Cursor conversation. Confirm "[Cursor]" badge appears on matching results.
    4. **Highlighting:** Confirm the query word appears highlighted (yellow/bold) in the result title.
    5. **Open Claude conversation:** Select a [Claude] result and press Enter. Confirm a NEW Terminal.app window opens and starts running `claude --resume ...`. rsight should remain in the original terminal.
    6. **No regression — files:** Switch to Files tab, search a filename, press Enter. Confirm file opens in $EDITOR.
    7. **No regression — folders:** Switch to Folders tab, search a folder name, press Enter. Confirm folder opens in Finder.
    8. **No regression — contents:** Switch to Contents tab, search a word, press Enter. Confirm file opens at the correct line.
    9. **Ctrl+C on AI result:** Select a conversation result, press Ctrl+C. Confirm the path is copied (status bar shows "Copied: ...").
    10. **Performance:** Results in AI Conversations tab appear within a few seconds (not 30+ seconds).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification covers all Phase 3 success criteria. No automated end-to-end test is feasible for TUI rendering and Terminal.app window spawning.
</verification>

<success_criteria>
All 10 verification steps pass. Phase 3 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-conversations-open-actions/03-04-SUMMARY.md`
</output>
